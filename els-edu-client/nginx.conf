server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        
        # This handles client-side routing - all routes serve index.html
        try_files $uri $uri/ /index.html;
    }
}